{
  "name": "zapasnoy-aerodrom-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/db.js && node --check src/telegram.js && node --check src/utils.js && node --test test/*.test.js",
    "dev": "wrangler dev --persist-to=/private/tmp/prodigital-work/cloudflare/zapasnoy-aerodrom-bot",
    "migrate:local": "wrangler d1 migrations apply zapasnoy-aerodrom-bot-db --local --persist-to=/private/tmp/prodigital-work/cloudflare/zapasnoy-aerodrom-bot",
    "migrate:remote": "wrangler d1 migrations apply zapasnoy-aerodrom-bot-db --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": { "node": ">=20" }
}

